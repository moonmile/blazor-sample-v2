@page "/lifegame1"
<h3>ライフゲーム</h3>
<svg width="100" height="100">
    @for (int y = 0; y < 10; y++)
    {
        for (int x = 0; x < 10; x++)
        {
            <rect x="@(x*10)" y="@(y*10)" width="8" height="8"
                  fill="lightgray" />
        }
    }
</svg>
